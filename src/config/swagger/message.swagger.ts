import { OpenAPIRegistry } from "@asteasolutions/zod-to-openapi";

export const registerMessageSwaggerPaths = (_registry: OpenAPIRegistry) => {
  // Chua co endpoint message HTTP de dang ky trong swagger.
};
